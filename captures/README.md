# Packet captures

Annotated Wireshark captures collected in milestone 8 land here:

- `dhcp-dora.pcapng` — full Discover/Offer/Request/Ack exchange
- `dns-lookup.pcapng` — client query against the domain resolver
- `kerberos-logon.pcapng` — AS/TGS exchanges followed by SMB session setup

Each capture gets a short `.md` note alongside it identifying the packets that matter and what they prove.
